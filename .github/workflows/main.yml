name: CI
on: [push]

defaults:
  run:
    shell: bash

jobs:

  # Check non-regression tests
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          cargo test